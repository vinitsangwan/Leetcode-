class Solution {
public:
    int peakIndexInMountainArray(vector<int>& arr) {
        int idx=0;
        int maxi=0;
        for(int i=0;i<arr.size();i++){
        maxi=max(arr[i],maxi);
        if(arr[i]==maxi){
            idx=i;
        }
        }
        return idx;
    }
};

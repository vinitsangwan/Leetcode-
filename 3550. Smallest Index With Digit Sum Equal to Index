class Solution {
public:
    int smallestIndex(vector<int>& nums) {
    for(int i=0;i<nums.size() ;i++){
        if(i==nums[i]%10+(nums[i]/10)%10+(nums[i]/100)%10+(nums[i]/1000)%10) return i;
    }
    return -1;
        
    }
};
